# 模板使用规范（草稿）

本文件说明使用【笔记写作】模板时的**结构约定与技术约束**。正文本身怎么写，见《正文写作规范》。

## 一、主文件的结构

`main.tex` 的固定骨架：

```latex
\documentclass[UTF8,10pt, a4paper, oneside,scheme=plain]{ctexbook}

\usepackage{./structure}
\usepackage{quiver}

\author{逐影}
\title{\Huge\textbf{书名}}
\date{\today}

\begin{document}

\frontmatter
\maketitle
\tableofcontents
\mainmatter

\part{……}

\input{./Content/1_Chapter_Name/index}

% ……

\backmatter

\end{document}
```

## 二、`\part{}` 的用法

`\part{}` 是 `ctexbook` 的「部」，用于把若干章归为一组。现有笔记有两种用法。

### 形式一：一章一部（多数笔记采用）

```latex
\part{数学的形式化描述}

\input{./Content/1_Description_of_Formal_Mathematic/index}

\part{集合论}

\input{./Content/2_Set_Theory/index}
```

`\part{}` 的标题与紧随其后那章的 `\chapter{}` 标题相同——相当于把章名提前显示在部页上。

### 形式二：多章一部

`OrthogonalMethod` 用一个 `\part` 覆盖四章，更贴近「部」的原义：

```latex
\part{正交集方法}

\input{./Content/1_Orthogonal_Method/……}
% 其余三章同属此部
```

### 约定

- `\part{}` 的标题用**中文**，与该章目录的英文名对应。
- 每行 `\part{}` 上方宜加注释写出英文名，便于对照：

  ```latex
  %Topological Structure
  \part{拓扑结构}
  \input{./Content/1_Topological_Structures/index}
  ```

- 暂不编译的章，把 `\part` 与 `\input` **两行一起**注释掉。
- `\part` 与 `\input` 之间空一行，保持文件可读。

## 三、定理环境的技术约束

1. 环境**必须带两个参数**：

   ```latex
   \begin{definition}{标题}{label}
   ```

   只写 `\begin{definition}` 会让 LaTeX 把正文片段吞作参数，写出非法标签。首轮编译不报错但会写坏 `.aux`，**第二轮编译时爆出大量错误**。

2. **label 平时留空** `{}`，仅当该条需要被引用时才填写。

3. **label 前缀**由 `\newtcbtheorem` 的第 4 参数自动添加。该参数是**直接字符串拼接、不自动加分隔符**，因此必须写成 `{def:}` 而非 `{def}`——后者生成 `defxxx`，与正文引用的 `def:xxx` 对不上，表现为「编译无错但 PDF 里引用全是问号」。

4. 已知前缀：

   `def:` `axi:` `hypo:` `thm:` `lem:` `prop:` `cor:` `metathm:` `crit:` `prob:` `ex:` `rmk:` `exer:` `algo:` `conv:` `alarm:` `sketch:`

5. 证明用 `\begin{proof} … \end{proof}`，环境自动在末尾加证毕符号 ∎。

## 四、编译

- 须用 **XeLaTeX**（`structure.sty` 依赖 ctexbook 与 XeLaTeX 特性），**不支持 pdfLaTeX**。
- `\usepackage{./structure}`（`main.tex`）与 `\ProvidesPackage{./structure}`（`structure.sty`）是**配套写法**：`./` 须同期存在或同期省略。只改一边会触发 `requested package` 警告；全去掉 `./` 则部分编辑器（如 TeXStudio）会误报「包未找到」。
- 交叉引用需**编译两遍**：第一遍的 undefined 会在第二遍消解，属正常现象。

## 五、符号库

- 正文中一律使用 `structure.sty` 提供的宏，不写裸命令（详见《正文写作规范》第五、六节）。
- 新增符号：改 `structure.sty` 后运行 `python symbols.py --all --write`（回填 + 刷新补全 + 分发）。
- **不能只跑 `--distribute`**：编译读 `structure.sty`、TeXStudio 读 `custom.cwl`，两套文件须同时刷新，否则会出现「编译正常但编辑器报未知命令」。刷新后需**重启 TeXStudio** 才生效。

## 六、跨文档引用

习题集（`ExerciseBook/`）引用笔记中的定理时，一律写 `\noteref{标签}`：

| 模式 | 展开为 | 机制 |
|---|---|---|
| 模式 1（独立习题集） | `\ref{note-标签}` | 跨文档引用，依赖 `xr-hyper`，须先编译笔记生成 `.aux` |
| 模式 2（讲义） | `\ref{标签}` | 本地引用 |

两种模式下习题文件**完全可移植**，切换模式时无需改动习题源码。
