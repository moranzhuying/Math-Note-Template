# 正文写作规范（草稿 · 第 3 版）

本文件约定正文的写法。所列条目均取自现有笔记的实际做法，供撰写时统一遵循。

## 一、文件与标题

1. 正文按 **Chapter → Section → 小节文件** 三级组织，每级目录各有一个 `index.tex` 汇总 `\input`。
2. 每个**节目录的第一个文件**写两行标题：

   ```latex
   \chapter{节目录名的中译}

   \section{该文件名的中译}
   ```

   同一节目录的**其余文件**只写 `\section{...}`。

   `\section{}` **优先写文件名的中译**；若直译不切合文件主题，则改用**切合该文件主题**的标题。
3. 标题内的数学符号用 `\texorpdfstring` 包装：

   ```latex
   \section{\texorpdfstring{$\R^{n}$}{Rn}的拓扑}
   ```

   **但优先避免这种写法。** 当纯文本参数无法正确表达上标或下标时（如 `$\R^{n}$` 只能写成 `Rn`），PDF 书签会失真。此时应优先考虑：

   - 给该**文件与节目录**取一个**切合主题**的名字，使其无需数学装饰即可表意；
   - 标题改用**中文陈述**，直接描述该节内容。

   可参考 `Algebra/Content/7_Polynomials_and_Rational_Fractions` 的写法——该章标题全部为中文陈述，不含数学符号：

   ```latex
   \section{多项式的定义}
   \section{多项式的代入}
   \section{多项式的微分和导数}
   \section{一元多项式环中的Euclid除法}
   ```

## 二、标点

1. 正文一律使用**英文标点**（`,` `.` `:` `;` `(` `)`），不使用中文标点。
2. 并列项之间用**英文逗号**，不用顿号。例如标题「拓扑,开集」、正文「开球,闭球,球面」。
3. 句末用 `.`，即便前一个字符是数学模式的 `$`：

   ```latex
   设$U\subseteq\R^{n}$.我们称……
   ```

## 三、陈述环境

### 环境名与语义

| 语义 | 环境 | 语义 | 环境 |
|---|---|---|---|
| 定义 | `definition` | 例 | `example` |
| 公理 | `axiom` | 注记 | `remark` |
| 假设 | `hypothesis` | 问题 | `problem` |
| 定理 | `theorem` | 算法 | `algorithm` |
| 引理 | `lemma` | 约定 | `convention` |
| 命题 | `proposition` | 警示 | `alarm` |
| 推论 | `corollary` | 习题 | `exercise` |
| 元定理 | `metatheorem` | 准则 | `criteria` |
| 分析 | `sketch` | | |

### 各类环境写什么

#### `definition`——引入新概念

第一次给某个对象命名时用，标题即该概念的名字：

```latex
\begin{definition}{连通空间,不连通空间}{}
	如果$X$不能表示成两个不相交非空开集的并,则称$X$是连通空间,否则称$X$是不连通空间.
\end{definition}

\begin{definition}{泛闭映射}{}
	若对每个拓扑空间$Z$,映射$f\times\id_{Z}$是闭映射,则称$f$是泛闭映射.
\end{definition}
```

**对偶概念**（连通／不连通、开／闭）用括号并列在相应位置，不必另立一条定义。

#### `theorem`——主要结果

构成本章或本节目标的结果。通常有通行名称，标题即用该名称（见下「标题怎么写」）：

```latex
\begin{theorem}{Zorn引理}{}
	……
\end{theorem}
```

#### `proposition`——辅助性结果

为通往主要结果服务的陈述，数量最多。可以是简单的观察，也可以是完整的等价刻画：

```latex
\begin{proposition}{泛闭映射的闭性}{}
	每个泛闭映射都是闭映射.
\end{proposition}

\begin{proposition}{连续单射是泛闭映射的等价条件}{}
	设$f$是连续单射,则$f$是泛闭映射$\iff$ $f$是闭映射$\iff$ $f$的余限制是同胚.
\end{proposition}
```

#### `lemma`——技术性结果

为证明某个定理而先建立、单独看无独立价值的结果。

#### `corollary`——直接推论

由紧邻的定理或命题直接导出、证明通常只有一两行的结果：

```latex
\begin{corollary}{Cauchy定理}{}
	……
\end{corollary}
```

#### `remark`——补充说明

`remark` 的用途**比「注记」二字所暗示的要宽**。实际正文中它承担以下几类内容：

| 内容 | 标题实例 |
|---|---|
| 记号说明 | `记号说明`、`序列的合成与结合的顺序有关` |
| 术语说明 | `术语说明`、`术语说明(重要!)` |
| **规定性的说明** | `括号使用规则`、`元语言代入规则` |
| 直观释义 | `演绎定理的直观语义释义`、`反证法的直观语义释义` |
| 具体计算 | `PID上半单模中非零元素的零化子的计算` |
| 特例与推广 | `典范嵌入的情形`、`二元拆分的情形` |
| 概念间的关系 | `正交群和酉群的关系`、`左右平移$\iff$结合性` |

**关于标题**：`remark` 的标题**多为空**（现有笔记 764 条 remark 中 581 条标题为空）。是否需要标题，取决于该条是否值得在正文中被单独指出：

- **留空**：补充性的说明，读者顺着读下来即可。
- **给出标题**：内容较多、或后续正文可能提及该条时，用简短的名词短语概括，如 `记号说明`、`术语说明`。

#### `convention`——默认约定

用于对**今后行文**作默认规定——某些未加说明的情况下，一律采用某种理解或某种记号。典型句式是「今后总是默认……」「未加说明时，默认……」「除非另有说明，否则……」：

```latex
\begin{convention}{}{}
	今后,论及拓扑群的子群,我们总是默认它是装备了子空间拓扑的拓扑群.
\end{convention}

\begin{convention}{}{}
	未加说明时,默认$\prod\limits_{i\in I}G_{i}$装备乘积拓扑.
\end{convention}

\begin{convention}{}{}
	我们称如此得到的拓扑群$S$为$N$和$L$关于$\sigma$的拓扑外半直积,今后总是默认它装备乘积拓扑.
\end{convention}
```

**`remark` 与 `convention` 必须分开**。二者的分界在于该内容是**对已述内容的补充**，还是**对今后行文的默认规定**：

| 判断 | 环境 |
|---|---|
| 补充说明某个记号/术语/概念**已经**是什么意思 | `remark` |
| 规定**今后**遇到某种情形**默认**怎么理解、怎么记 | `convention` |

现有笔记中有部分 `remark` 实际承担了 `convention` 的功能，标题写作 `记号约定`、`术语约定`，或正文含「今后，除非另有说明，否则……」——这类内容**宜改用 `convention`**。

#### `example`——例

举例说明或给出反例。

#### `sketch`——简略的推理

不适合作为正式证明、但需要向读者交代思路的地方：

```latex
\begin{sketch}{}{}
	由TVS的定义,$E$的加法和$\R$在$E$上的标量乘法都连续,由此直接得到结果.
\end{sketch}
```

- 标题与 label 一般**都留空**。
- 与 `proof` 的分工：`proof` 给完整证明（自动加 ∎）；`sketch` 只给要点（「由此直接得到结果」「结合连续性直接得到结果」）。
- 它有**独立编号**，需被引用时可填 label（前缀 `sketch:`）。

### 标题怎么写

标题写该条**内容的概括**，**不重复「定义」「命题」等类型词**——类型已由环境名给出。

| 环境 | 标题写 | 实例 |
|---|---|---|
| `definition` | 所定义的**概念名** | `{泛闭映射}`、`{连通空间,不连通空间}`、`{拓扑,开集}` |
| `proposition` / `theorem` | **结论的概括** | `{泛闭映射的闭性}`、`{连续单射是泛闭映射的等价条件}` |

#### 有通行名称的定理

若某条结果在文献中有通行名称，**标题直接用该名称**（这些名称本身已含「定理」「引理」等字，不必再加类型词）：

```latex
\begin{theorem}{Zorn引理}{}
\begin{theorem}{Krull定理}{}
\begin{theorem}{Jordan-Hölder定理}{}
\begin{theorem}{Euler-Lagrange定理}{}
\begin{corollary}{Cauchy定理}{}
\begin{proposition}{加强版的Zorn引理}{}
```

三点注意：

1. **环境类型与名称中的字面可以不一致**。名称是文献中的通行叫法，环境类型按该条在**本文论证中的角色**定。例如 `Zorn引理` 用 `theorem`、`Cauchy定理` 用 `corollary`，都属正常。
2. **必要的限定写在括号内**：
   ```latex
   \begin{theorem}{Witt延拓定理($\varepsilon$-Hermite型,二次型)}{}
   \begin{corollary}{有限元素族合成的Fubini定理(一般形式)}{}
   ```
3. **名称可与内容并列**，用英文逗号分隔：
   ```latex
   \begin{proposition}{子群指标的传递性,Lagrange定理}{}
   ```

### 正文怎么写

**`definition`** 有两种常见句式。

其一，「若……则称……」——条件在前，命名在后：

```latex
\begin{definition}{泛闭映射}{}
	若对每个拓扑空间$Z$,映射$f\times\id_{Z}$是闭映射,则称$f$是泛闭映射.
\end{definition}
```

其二，「设……若……则称……否则称……」——先设对象，再给条件与命名：

```latex
\begin{definition}{连通空间,不连通空间}{}
	如果$X$不能表示成两个不相交非空开集的并,则称$X$是连通空间,否则称$X$是不连通空间.
\end{definition}

\begin{definition}{连通集,不连通集}{}
	设$A\subseteq X$.若$A$是连通空间(相对的,不连通空间),则称$A$是$X$的连通集(相对的,不连通集).
\end{definition}
```

**`proposition` / `theorem`** 亦有两种句式。

其一，直接陈述（无前提）：

```latex
\begin{proposition}{泛闭映射的闭性}{}
	每个泛闭映射都是闭映射.
\end{proposition}
```

其二，「设……则……」——先交代前提，再给出结论：

```latex
\begin{proposition}{连续单射是泛闭映射的等价条件}{}
	设$f$是连续单射,则$f$是泛闭映射$\iff$ $f$是闭映射$\iff$ $f$的余限制是同胚.
\end{proposition}
```

### 分条与标号

一条陈述内部需要分条时，按下列三种情形处理：

| 情形 | 用法 | 编号 |
|---|---|---|
| 陈述含**多个并列结论** | `enumerate` | 自动编号 1.、2.、…… |
| 条内的**子条件**（供择一或并列满足） | `itemize` | 不带编号（圆点） |
| **公理组的条目** | 手工编号 | `($\mathrm{O}_{1}$)` 等（见第五节） |

多个并列结论：

```latex
\begin{proposition}{泛闭映射的局部化与拼接}{}
	设$f$连续.
	\begin{enumerate}
		\item 若$f$是泛闭映射,则对$Y$的每个子集$T$,映射$f_{T}$是泛闭映射.
		\item 设$\left(T_{i}\right)_{i\in I}$是$Y$的子集族,满足下列条件之一:
		\begin{itemize}
			\item $\left(\interior\left(T_{i}\right)\right)_{i\in I}$是$Y$的覆盖,
			\item $\left(T_{i}\right)_{i\in I}$是$Y$的局部有限闭覆盖.
		\end{itemize}
		若$\left(f_{T_{i}}\right)_{i\in I}$是泛闭映射族,则$f$是泛闭映射.
	\end{enumerate}
\end{proposition}
```

要点：

- 前提（「设 $f$ 连续.」）写在 `enumerate` **之前**，各分条共用。
- `itemize` 与其所属的 `\item` 之间不空行；`itemize` 结束后接续的句子与 `\item` 同级缩进。
- 引用某个分条时，在 `\cref` 后直接写编号（见第四节）。

## 四、交叉引用

1. 统一使用 `\cref{...}`，**不要**用 `\ref` 或 `\autoref`——`\cref` 会自动输出类型名（「命题 1.2」）。
2. 引用时写**带前缀的完整标签**：

   ```latex
   沿用\cref{prop:construction-of-standard-total-ordering-over-R}的设定.
   ```

3. **不要**在 `\cref` 前手写「命题」「定义」等类型词——无论是 `命题\cref{...}` 还是 `命题(\cref{...})` 都属重复，`\cref` 已自动输出类型名，手写会变成「命题命题 1.2」。
4. 引用某条陈述的**某个分条**时，在 `\cref` 后直接写该条的编号：

   ```latex
   如果\cref{thm:existence-and-uniqueness-of-completion}(1)的条件成立,则……
   ```

5. 已知前缀（由环境自动添加）：

   `def:` `axi:` `hypo:` `thm:` `lem:` `prop:` `cor:` `metathm:` `crit:` `prob:` `ex:` `rmk:` `exer:` `algo:` `conv:` `alarm:` `sketch:`

6. **承接前文的句式**——「沿用……的记号」「沿用……的设定」，二者含义不同，按需选用：

   | 写法 | 继承的内容 | 例句 |
   |---|---|---|
   | 沿用 `\cref{...}` 的**记号** | **符号命名**：前文为某些对象指定的名字 | 沿用 `\cref{prop:substitution-of-polynomials}` 的记号. |
   | 沿用 `\cref{...}` 的**设定** | **假设与构造**：前文对对象所作的假设、给定的结构 | 沿用 `\cref{prop:construction-of-associated-ordered-monoid-and-group}` 的设定. |
   | 沿用 `\cref{...}` 的**记号和设定** | 两者兼有 | 沿用 `\cref{prop:construction-of-canonical-topology-over-finite-dimensional-real-vector-space}` 的记号和设定. |

   选用要点：接下来的论述若**只用到前文引入的符号**，写「记号」；若还要**依赖前文给出的前提**（如「$X$ 是拓扑空间」「$\varphi$ 非退化」），写「设定」；两者都依赖时写「记号和设定」。

7. **跨文档引用**：习题集（`ExerciseBook/`）中引用本笔记的定理、定义时，一律写 `\noteref{标签}`，不要直接写 `\cref` 或 `\ref`：

   ```latex
   由\noteref{thm:existence-and-uniqueness-of-completion}可知……
   ```

   该宏在两种习题编排模式下自动展开：模式 1（独立习题集）展开为跨文档引用 `\ref{note-标签}`，模式 2（讲义）展开为本地引用 `\ref{标签}`。因此习题文件在两种模式下**完全可移植**。

   注意 `\noteref` **不带类型名**——它只输出编号。若需要类型名（如「定理 1.2」），需在 `\noteref` 前自行写出。

## 五、条文编号

一条陈述内部若需分列若干**条件**，每条条件用括号包裹的编号标识，形如 `($\mathrm{O}_{1}$)`：

```latex
\begin{definition}{拓扑,开集}{}
	设$\mathfrak{O}\subseteq\mathcal{P}\left(X\right)$.若$\mathfrak{O}$满足下列条件,则称之为$X$上的拓扑,……
	\begin{enumerate}
		\item ($\mathrm{O}_{1}$)$\forall\mathfrak{S}\subseteq\mathfrak{O}\left(\bigcup\limits_{S\in\mathfrak{S}}S\in\mathfrak{O}\right)$.
		\item ($\mathrm{O}_{2}$)$\forall\mathfrak{S}\in\fin\left(\mathfrak{O}\right)\left(\bigcap\limits_{S\in\mathfrak{S}}S\in\mathfrak{O}\right)$.
	\end{enumerate}
\end{definition}
```

### 编号的构成

编号写作 `(\mathrm{O}_{下标}^{上标})`，三部分各有含义：

| 部分 | 写法 | 含义 |
|---|---|---|
| 字母 | `\mathrm{O}` | 该组条件的**类别标识**，`O` 表示公理（open sets 的公理组） |
| 下标 | `_{1}`、`_{1a}` | **是哪一条**：只有数字表示第 1 条；数字＋小写字母表示第 1 条的 a 部分 |
| 上标 | `^{\prime}` | 该条的**另一种等价陈述**；无上标即原始陈述 |

### 上标 prime 的含义与计数

- **无上标**：原始陈述。如 `($\mathrm{O}_{1}$)`。
- **一个 `\prime`**：该条的**第 1 种**等价陈述。如 `($\mathrm{O}_{1}^{\prime}$)` 是 `($\mathrm{O}_{1}$)` 的等价陈述。
- **两个 `\prime`**：第 2 种等价陈述。如 `(\mathrm{O}_{1}^{\prime\prime})`。
- 等价陈述**至多两种**，因此上标最多为 `\prime\prime`。

### 若干实例

```latex
($\mathrm{O}_{1}$)                  % 原始陈述
($\mathrm{O}_{1}^{\prime}$)         % 第 1 种等价陈述
($\mathrm{O}_{1}^{\prime\prime}$)   % 第 2 种等价陈述
($\mathrm{O}_{3}$)、($\mathrm{O}_{3}^{\prime}$)   % 同一节的原始陈述与其等价陈述
```

分类的条件若进一步细分，用小写字母下标区分：

```latex
($\mathrm{O}_{2a}$)、($\mathrm{O}_{2b}$)   % 第 2 条拆成 a、b 两部分
```

### 类别字母

字母是该组条件的**类别标识**，按所述主题取名，同一篇笔记内保持一致。已使用的有：

| 标识 | 含义 |
|---|---|
| `\mathrm{O}` | 公理组（如拓扑的公理 ($\mathrm{O}_{1}$)、($\mathrm{O}_{2}$)） |
| `\mathrm{RO}` | 序关系公理组（如 ($\mathrm{RO}_{1}$)：$x\leq y\wedge y\leq z$ 蕴含 $x\leq z$） |

> 其余标识随章节主题扩展；同一类别在不同章节中应沿用同一个字母。

### 为什么用 `\mathrm`

标识一律取**正体**（`\mathrm{O}`），不用斜体 `O`：

- 它是这条条件的**名字**，不是数学对象，正体写法与「定义」「定理」等标签的性质一致；
- 正体可与正文中作变量的斜体 $O$、$X$、$E$ 等**明确区分**，避免读者把 ($\mathrm{O}_{1}$) 误读成关于对象 $O$ 的断言。

括号同样用正体：`($\mathrm{O}_{1}$)`。

## 六、记号约定

每节的**开头**可用一句「记号约定」声明本节通用的对象与记号，避免在每条陈述中重复假设。

### 形式一：条件较少时写单行

以 `记号约定:` 起头，多个条件之间用英文逗号连接：

```latex
记号约定:$G$是交换拓扑群.

记号约定:$G$是拓扑群(乘法记号,幺元为$e$),$\mathfrak{B}=\mathcal{N}\left(e\right)$.

记号约定:$X,Y$是拓扑空间,$\left|P\right|=1$,$\left(X_{i}\right)_{i\in I},\left(Y_{i}\right)_{i\in I}$是拓扑空间族.
```

补充性的限定写在括号内（如「乘法记号,幺元为 $e$」），与主句并列。

### 形式二：条件较多时分点列举

条件本身较复杂、或需要分组说明时，`记号约定:` 单独成行，后接 `itemize`：

```latex
记号约定:

\begin{itemize}
	\item $X$是拓扑空间,$R$是$X$上的等价关系,$C$是相对$R$的二元关系,$\varphi:X\to X/R$是典范满射.
	\item $X^{\prime}$是$X$的单点紧化,无穷远点是$\omega$,$R^{\prime}$是$X^{\prime}$上的等价关系.
\end{itemize}
```

**何时分点**：一行能写完（约 2～3 个条件）就用形式一；条件超过三个、或各条件各自成组时用形式二。

### 书写格式

**同类对象合并进一个数学模式**，逗号写在 `$...$` 内部：

```latex
记号约定:$E,F$是原群,配备运算律$\odot$.          % 正确
记号约定:$E$,$F$是原群,配备运算律$\odot$.        % 错误
记号约定:$m,n,p,q\in\N$,$I,J,K$是有限全序集.     % 正确
```

**不同对象各自的陈述**之间，逗号写在数学模式外部：

```latex
记号约定:$A$是PID,$K=\Frac(A)$.
记号约定:$\left(A,+,\cdot,\leq\right)$是有序域,$\mathrm{H}^{+}$是$E$上位似比$>0$的位似组成的集合.
```

上例中 `$A$是PID` 与 `$K=\Frac(A)$` 是两个对象各自的陈述，故逗号在外；而 `$E,F$` 是同一类对象的并列，故合并入一个数学模式。

**整条约定只用一个句号收尾**，中间各条件用逗号连接，不要用句号断成多句：

```latex
记号约定:$E,E^{\prime}$是集合;当$E$是原群时,默认$E$上的运算是$\odot,\top,+,\cdot$中的一种.   % 正确
记号约定:$E$,$E^{\prime}$是集合.当$E$是一个原群时,默认$E$上配备的运算是$\odot$,$\top$,$+$,$\cdot$中的一种.   % 错误（逗号位置 + 断成两句）
```

### 列举条件的顺序

按**依赖关系**排列——先引入被依赖的对象，再引入依赖于它的对象：

```latex
记号约定:$X$是拓扑空间,$R$是$X$上的等价关系,$C$是相对$R$的二元关系,$\varphi:X\to X/R$是典范满射.
```

上例的顺序是 `X` → `R`（$X$ 上的关系）→ `C`（相对 $R$ 的关系）→ `φ`（到 $X/R$ 的映射）：每一步都只引用前面已声明的对象。**不要**先写 `φ` 再写它依赖的 `X/R`。

### 覆盖范围

- 记号约定**只管本节**：每节需要时各自重写一遍，不假定读者记得上一节的约定。
- 若某对象在全章通用，仍在本节声明一次；跨节承继时用「沿用」（见第四节第 6 条）。
- 约定中只声明**对象与固定记号**（哪些空间、哪些映射、哪些族的范围），不要把待证结论或临时假设写进去。
- 无需声明记号时，也可用一句自然语言说明本节范围：

  ```latex
  本节接下来的内容中,默认$X$和$Y$都配备了拓扑.
  ```

## 七、括号与斜杠

### 括号 `()`

正文（含环境标题）中的**英文半角括号** `()` 有三种用途。注意与数学模式内部的 `\left(…\right)` 区分——后者是数学括号，不属此列。

**其一，标示可选或可省略的成分**——常见于标题：

```latex
(有向)角的交叉等价性
(半)双线性型的坐标表示
仿射空间中(半)直线之间的夹角
```

括号内的成分去掉后句子仍成立，加上则限定了适用范围。

**其二，列出对偶概念**——以「相对的」或「相对地」起头，跟在主概念之后：

```latex
则称$A$是$X$的连通集(相对的,不连通集).
设$A\subseteq X$.若$A$是闭集(相对的,开集)……
```

对偶的若干分支用英文逗号并列：

```latex
(相对的,闭集)
(相对地,满射,双射)
(相对地,右,双边)
```

**其三，补充说明**——对前述对象作限定或注释：

```latex
记号约定:$G$是拓扑群(乘法记号,幺元为$e$),……
记号约定:$\Delta$是交换群(加法记号),…….
```

### 斜杠 `/`

`/` **只用于数学模式内部**，表示**商结构**：

```latex
$X/R$        商空间
$G/H$        商群
$\Z/2\Z$     剩余类环
$E/\sim$     等价关系下的商集
$p:E\to E/R$
```

**正文的文字部分不使用 `/`**。不写「连通/不连通」「开/闭」这类表述，而写「连通与不连通」「开集与闭集」；并列关系用英文逗号或「与」。

## 八、强调与标记

正文中只用两种标记：`\textbf` 与 `\underline`。

**ctex 下的字体映射**（决定了这两个标记的视觉效果）：

```latex
[Script={CJK}, BoldFont={SimHei}, ItalicFont={KaiTi}]
```

| 标记 | 西文效果 | 中文效果 |
|---|---|---|
| `\textbf{...}` | 粗体 | **黑体** |
| `\textit{...}` | *斜体* | 楷体 |

### `\textbf{...}`——标示术语名

用于**引言或定义正文中首次出现的术语**，以及**列举各条目时条目的名字**（中文显示为黑体）：

```latex
一个理论$\mathcal{T}$的\textbf{字母表}由如下三类符号组成:
	\begin{enumerate}
		\item \textbf{逻辑常元}:$\square,\tau,\vee,\neg$.
		\item \textbf{变元}.
		\item \textbf{非逻辑常元}.
	\end{enumerate}

形式理论$\mathcal{T}$中的\textbf{表达式}(或\textbf{组合},\textbf{符号串})是指由$\mathcal{T}$的符号并排书写而成的有限序列.
```

并列的术语各自加粗：`\textbf{表达式}(或\textbf{组合},\textbf{符号串})`。

### `\underline{...}`——强调，或标示正在讨论的词本身

用于**强调关键判断**（尤其是否定式的强调），以及**把某个词作为词本身来讨论**——此时通常与引号同用：

```latex
这些符号\underline{不属于}形式数学.
它们的使用对于理论来说\underline{并非不可或缺}.

通过语意滥用,我们经常会说这些符号``\underline{就是}''表达式,而不是说它们``\underline{代表}''组合.
```

### 书名——用 `\textit{}`

正文**不使用书名号《》**（那是中文标点，与英文标点体系不合）。提及书名时，无论中外文，一律用 `\textit{}`：

```latex
复变量函数与实变量函数一起,是迄今最重要的函数,将在\textit{微分流形与解析流形}中作更深入的研究.
```

- **中文书名**：`\textit{微分流形与解析流形}`——在 ctex 下渲染为**楷体**，与正文有视觉区分。
- **西文书名**：`\textit{Éléments de mathématique}`——渲染为真正的斜体。

**提及本笔记自身的章节时不用任何符号**，直接写「第 6 章」「本章」，或用 `\cref` 引用。

### 不使用的标记

| 标记 | 说明 |
|---|---|
| **书名号《》** | 不使用。书名改用 `\textit{}`（见上）。 |
| **波浪线** | 不使用。 |
| **字体颜色** | 不使用。定理环境的配色由 `structure.sty` 统一设定，正文不手动着色。 |

### 数学中的 `\underline`

分次代数的语境下，`\underline{X}` 是一种数学记号（如 `\underline{\Hom}_{A}`、`\underline{\End}_{\Delta-A}`），与上述强调用法无关，按该领域的惯例书写即可。

## 九、行文习惯

1. 段落缩进使用 **Tab**（与现有文件保持一致，不要混用空格）。
2. 定义中需要分条列举时用 `enumerate`/`itemize`；各条的编号按第五节的规定书写，并与正文引用保持一致。

## 十、术语滥用与记号滥用

### 什么是"宣布过的滥用"

数学写作中有些简化是**必要的**——若拒绝一切"滥用"，文字会冗长到无法阅读。正确的做法不是禁用，而是**宣布**：在第一次使用处写明"这里我们滥用记号……"，此后即可放心沿用。

危险不在滥用本身，而在**宣布只发生一次**，且可能发生在很远的地方。第二次遇到同一个符号时，读者已经无处可查。

### 六种常见模式

下表可直接当检查表用：

| 模式 | 举例 | 危险在哪 |
|---|---|---|
| **与标准像认同** | 「在典范等同的意义下这个命题也成立」 | 此后同一符号同时是"$X$"和"$Y$ 中的那个 $X$"，读者分不清说的是哪个 |
| **省略结合括号** | 写 $\bigotimes_{i\in I}E_{i}$ 而不加括号 | 靠一个典范同构合法化，但那个同构从未被显式指出 |
| **映射与其诱导映射同字母** | $G/H$ 上的映射仍叫 $f$ | 商结构、上同调里遍地都是 |
| **结构与承载集同字母** | 「有限长度 $\Z$-模（视为群后）就是有限 Abel 群」 | 类型永远对不上，而这是刻意的 |
| **函数与其延拓同字母** | 「连续延拓到 $\hat{G}$」 | 定义域悄悄变了 |
| **$f^{-1}$ 的两种用法** | 逆映射 vs 逆关系 | 后者可以不对任何东西有像 |

### 宣布滥用的句式

正文中的实际写法有以下几种，按场景选用：

| 句式 | 实例 |
|---|---|
| 「滥用记号，将 $X$ 记作 $Y$」 | 我们通常滥用记号,将该$A$-模记作$A/\mathfrak{a}$. |
| 「滥用记号，记 $X\coloneq Y$」 | 滥用记号,记$z^{\ast}\lrcorner t\coloneq\theta_{\mathsf{T}\left(M\right)}\left(z^{\ast}\right)\lrcorner t$. |
| 「为了简便，我们滥用记号……」（放在句首，说明动机） | 为了简便,我们通常会滥用记号,直接用$\sum\limits_{n=0}^{\infty}x_{n}$表示级数$\left(\left(x_{n}\right)_{n=0}^{\infty},\left(s_{n}\right)_{n=0}^{\infty}\right)$. |
| 「一般滥用符号记作 $X$」（简短形式） | $E$的子群$\left\{0\right\}$是$E$的子模,称为零子模,一般滥用符号记作$0$. |
| **`\textbf{滥用记号}`**（强调，用于较易混淆处） | 虽然$N\neq 0$时$g$的定义域只是$\mathbb{P}\left(V\right)$的子集,我们依然\textbf{滥用记号},将其记作$g:\mathbb{P}\left(V\right)\to\mathbb{P}\left(V^{\prime}\right)$. |

**要素**：触发词（滥用记号／视为／典范等同）＋ 认同的内容（把什么当成什么）＋ 新记号。三者缺一不可——只说"滥用记号"而不说认同了什么，等于没宣布。

### 触发词与类型检查

正文中出现下列词时，**该处必有认同**，须确认它已被宣布过；若尚未宣布，就在此处宣布：

| 类别 | 触发词 |
|---|---|
| 认同 | 典范等同、典范地视为、典范嵌入、视为、等同为、认同 |
| 滥用 | 滥用记号、滥用符号 |
| 省略 | 无歧义时、没有歧义的时候、简记、简写 |
| 引申 | 延拓、扩充、诱导（当指称的对象从原定义域扩大时） |

另一条判据不必依赖词表，可以直接从**类型**上看：

> **凡类型对不上——该是元素的地方出现了集合，或反过来——那一处大概率是已宣布的认同。** 往回找它宣布的位置：通常在节首的记号约定、或该条陈述的第一句话。

两条判据配合使用：**触发词负责"读到时留意"，类型检查负责"没读到触发词时也能发现"**。
