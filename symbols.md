# symbols.py

符号库管理面板：统一维护 `structure.sty` 中的自定义宏，并同步到各笔记与 TeXStudio 的补全文件 `custom.cwl`。

## 运行

```bash
python symbols.py                   # 进入交互式面板
```

直接运行（不带参数）即进入面板。

## 面板选项

运行后命令行窗口显示：

```
==============================================================
  笔记符号管理面板
==============================================================
  笔记根目录：<笔记所在目录>
  符号来源　：<模板目录>\structure.sty
  补全文件　：...\texstudio\completion\user\custom.cwl
  子文件夹　：10 个
--------------------------------------------------------------
  1. 设置笔记文件夹并扫描目录结构（记录结构，只更新有差异的部分）
  2. 显示子文件夹结构（按英文名排序编号，可多选查看）
  3. 提取各子文件夹的符号
  4. 回填提取的符号（先 structure.sty，再 custom.cwl）
  5. 检验并删除未使用的符号
  6. 引入新的记号
  7. 退出
--------------------------------------------------------------
  可多选，用英文逗号分隔（如 1,3,5）
```

| 选项 | 功能 |
|---|---|
| 1 | 设置笔记文件夹并扫描目录结构；与上次记录（`notes_tree.json`）对比，只更新有差异的部分 |
| 2 | 显示子文件夹结构：按英文名排序编号，可多选查看各文件夹的详情 |
| 3 | 提取各子文件夹正文中使用的符号，记录到 `symbols_extract.json` |
| 4 | 把提取到的符号回填至 `structure.sty`，再刷新 `custom.cwl` 补全 |
| 5 | 检验各符号是否被正文引用；对「所有笔记都未使用」的符号，可选逐项确认或按编号批量删除 |
| 6 | 交互式录入新的记号（命令名、定义必填，注释可选） |
| 7 | 退出面板 |

## 命令行模式

不带参数时进入面板；带参数则直接执行对应操作（需加 `--write` 才真正写入，否则仅预览）。

| 参数 | 功能 |
|---|---|
| `--all` | 回填 + 刷新补全 + 分发（三项全做） |
| `--merge` | 回填：各笔记 → 模板 |
| `--cwl` | 刷新 TeXStudio 补全 |
| `--distribute` | 分发：模板 → 各笔记 |
| `--drop NAME ...` | 从模板删除指定符号（可跟多个） |
| `--write` | 真正写入（不加则仅预览） |
| `--root` / `--template` / `--cwl-path` | 覆盖默认的根目录 / 符号来源 / 补全文件路径 |

例：

```bash
python symbols.py --all --write          # 回填 + 刷新补全 + 分发
python symbols.py --drop alt dr --write  # 删除 \alt 与 \dr
```

> 改动符号库后须用 `--all --write`（而非只 `--distribute`），否则会出现「编译正常但编辑器报未知命令」——因为编译读 `structure.sty`，而编辑器读 `custom.cwl`，两者需同时刷新。
